/tmp
/out-tsc
.DS_Store
/.idea

# Go build output
/dist

# npm-fetched / locally-built Go binary (bin/cvox.cjs launcher IS tracked)
/bin/cvox-bin
/bin/cvox-bin.exe

# node
/node_modules
*.tgz

# cvox hook probe log (forensic, local-only)
/probe.log
/probe-full.log

# cvox debug event log (written when config debug=true, local-only)
/.cvox.log
